# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BitCoinTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BitCoinTracker
  pod 'Alamofire', '~> 5.0.0-rc.2'
  target 'BitCoinTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitCoinTrackerUITests' do
    # Pods for testing
  end

end
